class Meal {
  constructor(side, mainDish, dessert) {
    this.side = side;
    this.mainDish = mainDish;
    this.dessert = dessert;
  }
  // makeFullDinner() {
  //   return `${this.mainDish} with a side of ${this.side} and ${this.dessert} for dessert!`;
  // }
}
